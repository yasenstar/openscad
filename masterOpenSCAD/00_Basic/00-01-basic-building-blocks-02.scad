/*
    This is an OpenSCAD test project
    -------------------------------------------------
*/

radius_with_a_name = 20; // a very important radius parameter

// the main sphere of our model
sphere(r = radius_with_a_name);
